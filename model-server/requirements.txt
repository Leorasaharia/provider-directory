fastapi>=0.95
uvicorn[standard]>=0.22
pandas>=2.0
torch>=2.2
pydantic>=1.10
python-multipart

# Notes:
# - For CPU-only deployments you can pin a CPU-specific torch wheel, for example:
#   torch==2.2.2+cpu -f https://download.pytorch.org/whl/cpu/torch_stable.html
# - If you deploy on Render/Cloud Run, use this file in the build step:
#   pip install -r model-server/requirements.txt
