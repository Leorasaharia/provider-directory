# Provider-directory Flow_1 Python dependencies (inferred from code)
# Install with: pip install -r Flow_1/requirements.txt

fastapi
uvicorn
pydantic
python-dotenv
requests
beautifulsoup4
lxml
rapidfuzz
bs4
python-multipart
pydantic
requests

# Optional: only required if you want Gemini (Google Generative AI) PDF extraction
# google-generativeai==0.2.0

# Standard library modules used by Flow_1 (no install needed):
# typing, io, zipfile, csv, json, os, re
